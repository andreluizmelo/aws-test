version: 0.1

phases:
  build:
    commands:
      - npm install -g less
      - lessc styles/main.less styles/main.css
      - echo Nothing to do yet
artifacts:
  files:
    - index.html
    - favicon.ico
    - styles/main.css
    - images/*
